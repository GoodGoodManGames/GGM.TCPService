using DemoService.Service;
using GGM.Context;
using GGM.Context.Attribute;
using SuperSocket.SocketBase.Protocol;

namespace DemoService.Configuration
{
    /*
    [Configuration]
    public class TCPConfiguration
    {
        [Managed(ManagedType.Singleton)]
        public IReceiveFilterFactory<TestPacketInfo> GetReceiveFilterFactory()
        {
            return new DefaultReceiveFilterFactory<ReceiveFilter, TestPacketInfo>();
        }
    }
    */
}